<h1>Ruby on Rails Test pages</h1>

<% @message.each do |message| %>
  名前：<%= message.name %>
  内容：<%= message.body %>
  <br>
<% end %>  


<%= form_for(Message.new) do |f| %>
   name:
	<%= f.text_field :name %>
   contents:
	<%= f.text_field :body %>
	<%= f.submit %>
<% end %>

